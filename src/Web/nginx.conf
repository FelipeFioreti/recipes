server {
    listen 80;
    server_name chefarchive.felipe-fioreti.tech;

    root /usr/share/nginx/html;
    index index.html;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /api/ {
        proxy_pass http://api:8080/api/;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

# server {
#     listen 443 ssl;
#     server_name chefarchive.felipe-fioreti.tech;
# 
#     ssl_certificate /etc/nginx/certs/live/chefarchive.felipe-fioreti.tech/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/live/chefarchive.felipe-fioreti.tech/privkey.pem;
# 
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers off;
# 
#     root /usr/share/nginx/html;
#     index index.html;
# 
#     location /api/ {
#         proxy_pass http://api:8080/api/;
#         proxy_http_version 1.1;
# 
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# 
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }